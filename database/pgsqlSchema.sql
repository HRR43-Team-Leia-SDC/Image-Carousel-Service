-- Setup commands here (make sure to end enach command with a ';').  Also need to know how to trigger it.

-- something like this (need to check syntax for postgres):

-- CREATE TABLE images (
--   _id SERIAL PRIMARY KEY,
--   id INTEGER NOT NULL,
--   url VARCHAR NOT NULL,
-- )